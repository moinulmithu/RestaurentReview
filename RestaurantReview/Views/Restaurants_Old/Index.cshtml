
@*@model RestaurantReview.Models.RestaurantViewModel*@

@*@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach (var item in Model){
    <div>
        <h4>@item.Name</h4>
        <div>@item.City, @item.Country</div>
        <div>Reviews : @item.CountOfReviews</div>
    </div>
}*@



@model IEnumerable<RestaurantReview.Models.RestaurantViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>Name</th>
        <th>City</th>
        <th>Country</th>
        <th></th>
        <th>Count Reviews</th>

       @* <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.City)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Country)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CountOfReviews)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.City)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Country)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CountOfReviews)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id }) |
            @Html.ActionLink("Reviews", "Index", "RestaurantReviews", new { id=item.Id}, null)
        </td>
        <td>@item.Name</td>
        <td>@item.City</td>
        <td>@item.Country</td>
        <td>@item.CountOfReviews</td>
    </tr>
}

</table>
